<resources>

    <style name="Theme.Password_managerUI"
        parent="Theme.AppCompat.DayNight.NoActionBar">
    </style>

    <style name="Theme.WaultSplash"
        parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Password_managerUI</item>

    </style>

    <style name="Theme.Wault.TransparentSettings"
        parent="Theme.MaterialComponents.DayNight.Dialog">

        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>

    </style>

</resources>